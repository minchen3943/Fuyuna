# 应用运行环境 (development/production)
NODE_ENV= development

# PostgreSQL 数据库配置
POSTGRES_HOST=                  # 数据库主机地址
POSTGRES_PORT=                       # 数据库端口号
POSTGRES_USER=                   # 数据库用户名
POSTGRES_PASSWORD=             # 数据库密码
POSTGRES_DATABASE=                 # 数据库名称

# GraphQL 配置
GRAPHQL_AUTO_SCHEMA_FILE=  # 自动生成的GraphQL架构文件路径
GRAPHQL_SORT_SCHEMA=                 # 是否对生成的schema进行排序

# JWT 认证配置
JWT_SECRET=                    # JWT加密密钥
JWT_EXPIRES_IN=                        # Token有效期
JWT_ISSUER=                        # Token签发者
JWT_ALGORITHM=                      # 加密算法

# 腾讯云 COS 配置
TENCENT_COS_SECRET_ID=   # 腾讯云API的SecretId
TENCENT_COS_SECRET_KEY=      # 腾讯云API的SecretKey
TENCENT_COS_BUCKET_NAME=                # COS bucket名称
TENCENT_COS_REGION=                              # COS地域
